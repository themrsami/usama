import React from 'react'
import type { Metadata } from "next";

const metadata: Metadata = {
    title: "React",
    description: "React page including a list of projects",
  };

const ReactPage = () => {
  return (
    <h1>ReactPage</h1>
  )
}

export default ReactPage